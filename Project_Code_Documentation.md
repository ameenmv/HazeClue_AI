# توثيق الكود البرمجي لمشروع HazeClue AI (RARD–MVES v2.2)

هذا الملف عبارة عن شرح تفصيلي وشامل لكل سطر ووحدة (Module) في المشروع. تم إعداد هذا الملف لتسهيل فهم هيكل النظام للجامعة أو لأعضاء الفريق.

---

## 1. النظرة العامة والمبدأ الأساسي (Overview)
مشروع **HazeClue AI** هو نظام هجين للذكاء الاصطناعي (Hybrid AI) يقوم بتحليل إشارات تخطيط كهربية الدماغ (EEG) في الوقت الفعلي لتصنيف حالة "التركيز الذهني" أو "الراحة". النظام مصمم للعمل مع أجهزة منخفضة الكثافة ذات أقطاب جافة (مثل جهاز EMOTIV بـ 14 قناة).
المشكلة الأساسية التي يعالجها النظام هي عدم استقرار الإشارة (تشويش، انفصال أقطاب، حركة عضلات). لحل ذلك، يستخدم النظام خوارزمية **توجيه ذكي (Smart Routing)** تعتمد على جودة الإشارة لتقرر أياً من المسارات سيتم استخدامه:
1. **مسار RARD (الهندسة الريمانية):** في حال كانت الإشارة ممتازة (دقة عالية).
2. **مسار MVES (المسار الإحصائي):** في حال كانت الإشارة متوسطة أو بها بعض التشويش.
3. **مسار SAFE:** في حال كانت الإشارة سيئة جداً ويتم رفض التنبؤ.

---

## 2. الهيكل العام للمشروع (Project Structure)
يحتوي المشروع على عدة مجلدات أساسية، وكل مجلد مسؤول عن جزء من دورة حياة البيانات:

- **`main.py`**: ملف التشغيل الرئيسي.
- **`data/`**: التعامل مع تحميل وقراءة البيانات.
- **`preprocessing/`**: تنظيف ومعالجة الإشارات.
- **`routing/`**: التوجيه الذكي للمسارات.
- **`features/`**: استخراج الخصائص من الإشارة (Feature Extraction).
- **`training/`**: تدريب النماذج وتطويرها.
- **`inference/`**: محرك التنبؤ والعمل في الوقت الفعلي.
- **`export/`**: تصدير النماذج للعمل على الموبايل.

---

## 3. شرح الوحدات البرمجية (Modules Breakdown)

### أولاً: نقطة البداية (Entry Point) - `main.py`
هذا الملف هو المسؤول عن تجميع كل أجزاء المشروع لبدء التدريب.
- يبدأ بقراءة المُدخلات (Arguments) من سطر الأوامر (Command Line) مثل مسار الـ Dataset (STEW أو Mendeley) وعدد الفئات (Folds) المطلوبة في التدريب.
- يقوم بتهيئة (Initialize) فئة `HazeClueDataset` لتحميل البيانات من المسارات المحددة.
- يستدعي دالة `train_pipeline` للبدء في عملية التدريب وتقسيم البيانات وتجربة النموذج.
- أخيراً، إذا تم طلب تصدير الموديل، يقوم باستدعاء وظيفة `export_all_models` لإنشاء نسخ ONNX.

---

### ثانياً: معالجة البيانات (Data Module) - مجلد `data/`

**1. ملف `data/loaders/stew_loader.py`:**
- وظيفته قراءة ملفات النصوص (.txt) الخاصة بـ **STEW Dataset** (والتي تحتوي على 48 شخص بـ 128 هرتز).
- يجمع البيانات الخاصة بالـ "راحة" (`_lo.txt`) والـ "تركيز/العمل" (`_hi.txt`).
- يقوم بتقطيع البيانات إلى نوافذ (Windows) بحجم 4 ثوانٍ مع تداخل (Overlap) بنسبة 50%.

**2. ملف `data/loaders/mendeley_loader.py`:**
- يقرأ بيانات **Mendeley Dataset** وهي بصيغة `EDF`.
- البيانات هنا مسجلة بتردد 250 هرتز، فيقوم بعمل **Downsampling** لخفض التردد إلى 128 هرتز ليطابق مجموعة بيانات STEW.
- يتم تجاهل الجزء الخاص بإغلاق العينين بالكامل لأنه يؤثر على دقة النموذج (بسبب الـ Alpha Peaks). يتم فقط استخدام بيانات الراحة (أعين مفتوحة) والتركيز.

**3. ملف `data/dataset.py`:**
- يقوم بتجميع البيانات القادمة من كلا المصدرين (STEW + Mendeley).
- يحتوي على دوال لعمل الـ `GroupKFold` لضمان تقسيم البيانات بناءً على هوية الشخص (Subject). هذا يمنع ظاهرة التسريب (Data Leakage) حيث لا يمكن أن تكون بيانات الشخص موجودة في مجموعة التدريب ومجموعة الاختبار في نفس الوقت.

---

### ثالثاً: المعالجة المبدئية (Preprocessing) - مجلد `preprocessing/`

**1. ملف `preprocessing/bandpass.py`:**
- يحتوي على فلتر ترددي (Butterworth Bandpass Filter).
- يقوم بتصفية الترددات خارج النطاق 1 إلى 40 هرتز بطريقة صفري الطور (Zero-phase).
- تردد الـ 1 هرتز يُزيل الانحرافات الكهربائية (Drift)، وتردد الـ 40 هرتز يُزيل معظم الضوضاء الناتجة عن حركة العضلات.

**2. ملف `preprocessing/sqi.py` (Signal Quality Index):**
- هذا الكود مهم جداً؛ فهو يحسب "مؤشر جودة الإشارة" لكل قناة بشكل مستقل.
- يختبر الكود عدة أشياء: ثبات التباين، الوصول للحد الأقصى للمستشعر (ADC Saturation)، والإشارة المسطحة (Flatline).
- يعطي تقييماً من 0.1 إلى 1.0 (لا يُعطي 0 لكي لا تتلف المصفوفات الرياضية اللاحقة).

**3. ملف `preprocessing/covariance.py`:**
- يحسب "مصفوفة التغاير" (Covariance Matrix) للإشارة، وهو متطلب أساسي في الهندسة الريمانية.
- يتم وزن (Weighting) القنوات بناءً على جودة الـ SQI.
- يطبق تقنية `Ledoit-Wolf Shrinkage` لتحسين استقرار المصفوفة وتجنب المشاكل الرياضية (Singularity).

---

### رابعاً: التوجيه الذكي (Routing) - مجلد `routing/`

**ملف `routing/mode_router.py`:**
- يمثل عقل النظام في اتخاذ القرار في الوقت الفعلي.
- يحسب متوسط جودة الإشارة لكل الـ 14 قناة (SQI mean)، ويحسب مقياس Condition Number لمصفوفة التغاير.
- **المنطق (Logic):**
  - إذا كانت الجودة أعلى من 0.65 والمصفوفة مستقرة ← توجيه نحو **RARD** (الأعلى دقة).
  - إذا كانت الجودة بين 0.35 و 0.65 ← توجيه نحو **MVES** (يتحمل التشويش المعتدل).
  - إذا كانت الإشارة سيئة جداً ← **SAFE** (رفض التنبؤ لعدم الموثوقية).

---

### خامساً: استخراج الخصائص (Feature Extraction) - مجلد `features/`

**1. ملف `features/rard_features.py` (مسار الهندسة الريمانية):**
- يعمل في فضاء الـ SPD Manifolds.
- يقوم بعمل إسقاط (Projection) لمصفوفات التغاير إلى فضاء مماس (Tangent Space) باستخدام نقطة مرجعية (Fréchet Mean).
- يستخرج **105 خاصية (Features)** تمثل علاقات الارتباط والاتصال المكاني بين مناطق الدماغ المختلفة بأعلى درجات الدقة.

**2. ملف `features/mves_features.py` (المسار الإحصائي):**
- يعتمد على الخصائص التقليدية والمستقرة، ويولد **203 خاصية**.
- يحسب طاقة النطاقات (Bandpower) للترددات (Delta, Theta, Alpha, Beta).
- يحسب متغيرات Hjorth (النشاط، الحركة، التعقيد).
- يحسب معاملات الارتباط (Pearson Correlation) بين القنوات المختلفة.

---

### سادساً: التدريب (Training) - مجلد `training/`

**ملف `training/train.py`:**
- يدير عملية تدريب مصنفات التعلم الآلي.
- لضمان قوة النموذج في الحياة الواقعية، يقوم الكود بـ **حقن ضوضاء (Data Augmentation)** بنسبة 30% من العينات (يفصل بعض القنوات، يضيف تشويش).
- يستخدم خوارزمية **LDA (Linear Discriminant Analysis)** لمسار RARD، وخوارزمية **Logistic Regression** لمسار MVES.
- يعتمد التقييم على الـ Accuracy، و F1-Score، ومعامل Cohen's Kappa.

---

### سابعاً: محرك الاستدلال (Inference) - مجلد `inference/`

**1. ملف `inference/engine.py`:**
- محرك التوقع (Prediction Engine) الفعلي. يتم إعطاؤه نافذة (Window) جديدة من الإشارات العصبية، ليقوم بكل السلسلة السابقة (فلترة ← جودة ← توجيه ← خصائص ← تصنيف).
- يستخدم **Exponential Moving Average (EMA)** لتنعيم التوقعات ومنع التذبذب السريع في قرار النموذج، مما يجعله أكثر استقراراً للمستخدم (مثلاً يعطي وزن 70% للقرار الحالي و 30% للقرار السابق).

**2. ملف `inference/personalization.py`:**
- نظام "تكيف شخصي" (Calibration) يعمل على ضبط التوقعات بناءً على دماغ المستخدم الحالي.
- يستخدم طريقتين: الأولى سريعة على الموبايل وتُعدل التوقعات (Euclidean EMA)، والثانية بطيئة على السيرفر (Fréchet Mean Recentering) لإعادة ضبط النقطة المرجعية الريمانية.

---

### ثامناً: التصدير (Export) - مجلد `export/`

**ملف `export/export_onnx.py`:**
- بما أن التطبيق سيعمل في النهاية على الموبايل باستخدام بيئة Flutter، يجب تحويل النماذج التي دُربت بواسطة `scikit-learn` في بايثون إلى صيغة قياسية خفيفة.
- هذا الملف يستخدم مكتبة `skl2onnx` لتحويل جميع مصنفات الذكاء الاصطناعي (RARD و MVES) و Scalers إلى ملفات `.onnx`.
- الهدف من هذا هو الوصول إلى سرعة أداء (Latency) أقل من 35 مللي ثانية على الموبايل ليصبح مناسباً للتطبيقات ذات الزمن الفعلي (Real-time).

---

## 4. تدفق العمل للمبرمج (Developer Workflow)

لتجربة النظام وعمل بيئة تشغيل، تم استخدام الخطوات التالية في المشروع:
1. إعداد البيئة الافتراضية (Virtual Environment) باستخدام مكتبات معالجة البيانات العصبية مثل `mne` للتعامل مع الـ EDF، و `pyriemann` للهندسة الريمانية.
2. وضع البيانات في المسارات المناسبة:
   - `data/raw/stew/STEW Dataset/`
   - `data/raw/mendeley/Emotiv 30s EDF/`
3. تشغيل كود التدريب باستخدام: `python main.py --export-onnx`
4. ينتج عن هذا مجلد `trained_models` يحتوي على النماذج بصيغة `joblib` ومركز التغاير `P_ref.npy`. كما يتم استخراج نسخ الموبايل في مجلد `onnx_models`.
